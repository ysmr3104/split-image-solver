# 14mm超広角画像プレートソルブ: 現状報告と改善検討

## 1. 現状の実行結果

### テスト条件
- **カメラ**: Sony α7RV
- **レンズ**: 14mm 超広角
- **画像**: drizzle後 9728x6656px
- **中心ピクセルスケール**: ~54.4 arcsec/pixel
- **中心座標**: RA=274.3°, Dec=-14.9°
- **推定FOV**: ~147° x 101°（超広角）
- **インデックスファイル**: 4110-4119, 4214-4219（最大クワッドスケール ~35'）
- **search_radius**: 45°

### 8x8グリッド 1stパス結果: 11/64 タイル成功

| タイル | RA | Dec | スケール | FOV | 所要時間 |
|--------|------|------|----------|-----|----------|
| (2,3) | 268.39° | +0.87° | 49.27"/px | 24.3° | 2.9s |
| (2,4) | 255.02° | -10.21° | 49.55"/px | 24.3° | 3.8s |
| (3,2) | 288.64° | +3.32° | 50.32"/px | 26.6° | 17.5s |
| (3,3) | 276.44° | -7.91° | 52.78"/px | 22.2° | 2.8s |
| (3,4) | 262.43° | -19.64° | 52.61"/px | 22.2° | 12.1s |
| (3,5) | 247.77° | -29.32° | 48.86"/px | 26.6° | 94.6s |
| (4,2) | 296.51° | -4.74° | 49.79"/px | 26.6° | 7.7s |
| (4,3) | 285.12° | -16.70° | 51.51"/px | 22.2° | 4.4s |
| (4,4) | 271.00° | -28.98° | 52.82"/px | 22.2° | 2.7s |
| (4,5) | 255.32° | -38.49° | 48.96"/px | 26.6° | 34.1s |
| (5,3) | 294.00° | -24.78° | 48.42"/px | 24.3° | 11.7s |

### 成功パターンの分析

```
8x8グリッド配置（行=Y方向、列=X方向）:
OK=1stパス成功、ok=2ndパス成功、FP=偽陽性、x=失敗

     col0  col1  col2  col3  col4  col5  col6  col7
row0  x     FP    x     x     x     x     x     x     ← 上端（空方向最外縁）
row1  x     x     x     ok    x     x     x     x
row2  x     FP    x     OK    OK    ok    x     x
row3  x     x     OK    OK    OK    OK    x     x
row4  x     x     OK    OK    OK    OK    x     x
row5  x     x     x     OK    x     x     x     x
row6  x     x     x     x     x     x     x     x     ← 地上景
row7  x     x     x     x     x     x     x     x     ← 地上景
```

**成功タイルの特徴**:
- 全て画像中心付近（行2-5, 列2-5）に集中
- 実効FOVが ~22-27° の範囲内
- ピクセルスケールが 48-53"/px の範囲

**失敗タイルの分類**:
- **行0-1, 行6-7**: 画像上端（高歪み領域）/ 画像下端（地上景）
- **列0-1, 列6-7**: 画像左右端（高歪み領域）
- **行5の列0-2, 4-7**: 成功圏外の中間領域

### 2ndパス結果: +4タイル（うち偽陽性2）→ 実質 +2タイル

53失敗タイルをWCS由来RA/DECヒント + スケール制約なしでリトライ。所要時間: 約2時間10分。

| タイル | WCSヒント | 実測値 | スケール | 所要時間 | 判定 |
|--------|-----------|--------|----------|----------|------|
| (0,1) | RA=278.10° Dec=+29.68° | RA=316.72° Dec=+5.94° | 27.12"/px | 606s | **偽陽性** |
| (1,3) | RA=261.18° Dec=+8.73° | RA=261.14° Dec=+8.72° | 47.73"/px | 278s | 正常 |
| (2,5) | RA=241.72° Dec=-19.99° | RA=240.81° Dec=-19.88° | 48.77"/px | 83s | 正常 |
| (2,1) | RA=291.46° Dec=+18.89° | RA=287.85° Dec=-15.80° | 10.93"/px | 187s | **偽陽性** |

**実質成功: 13/64 (20.3%)** = 1stパス11 + 2ndパス真の追加2

### 2ndパスの問題点

1. **スケール制約の完全除去**: `fov_hint=None` で全スケールを探索させているため、1タイルあたり最大600秒かかる
2. **偽陽性**: スケール制約なしだとスケールが大きく異なる誤マッチが発生（tile (0,1) scale=27"/px、tile (2,1) scale=10.9"/px）
3. **極端に非効率**: 53タイル中49タイルが600秒タイムアウト → 約2時間で実質2タイルの追加
4. **地上景タイル**: 星がほぼ写っていないためどうしても失敗する

### WCS統合結果

偽陽性2タイルを含む15タイルで統合を実行:
- オーバーラップ検証: **max error = 4685"（不合格）** — 偽陽性タイルが大きく影響
- TAN+SIP精度: weighted mean = 69.34 arcmin（偽陽性除去で改善見込み）
- 推定FOV: 147.4°、SIP次数5

### 改善が必要な点

- **偽陽性フィルタ**: 2ndパス結果のスケールとWCSヒント座標の整合性チェックが必須
- **2ndパスのスケール制約**: 完全除去ではなく、成功タイルのスケールから推定した広めの範囲を指定すべき
- **タイムアウト短縮**: 2ndパスは120-180秒で十分（正確なRA/DECヒントがあるため）

---

## 2. 失敗の根本原因分析

### 原因1: 正距投影（gnomonic/TAN）の非線形性

14mm（FOV ~147°）では、画像端のピクセルスケールが中心と比較して**2倍以上**異なる:

```
中心:        54.4 arcsec/pixel
コーナー:   ~113 arcsec/pixel（2.1倍）
エッジ端:   ~87-95 arcsec/pixel（1.6-1.7倍）
```

タイルごとの補正済みスケールヒントを実装済みだが、solve-field のスケール範囲 ±20-40% では依然として不足するケースがある。

### 原因2: レンズ歪曲収差

14mm超広角レクティリニアレンズは顕著な**樽型歪曲収差**を持つ:
- 星のパターン（quad）がカタログと幾何学的に不一致
- タイル分割により歪みが不均一に分配される（中心タイルは歪み小、端タイルは歪み大）
- solve-field の SIP 多項式（デフォルト次数2）ではタイル内の局所歪みを十分にモデル化できない

### 原因3: インデックスファイルのクワッドスケール制限

| インデックスシリーズ | クワッドスケール範囲 | 対象FOV |
|-------|-------|-------|
| 4110 | 2-2.8 arcmin | ~10' |
| ... | ... | ... |
| 4119 | 22-35 arcmin | ~2-3° |
| 4214-4219 | Tycho2系 | 要確認 |

エッジタイルの実効FOVが30-40°の場合、タイル内に存在するクワッド（4星の組み合わせ）が35'より大きくなり、どのインデックスにもマッチしない。

### 原因4: 地上景

下端45%は地上景であり、星がほとんど写っていないためソルブ不可能。これは改善の余地がない。

---

## 3. カメラ・レンズ詳細情報

### 3.1 撮影機材

| 項目 | 値 |
|------|-----|
| **カメラ** | Sony α7RV (ILCE-7RM5) |
| **レンズ** | Sony FE 14mm f/1.8 GM (SEL14F18GM) |
| **撮影絞り** | F2.0 |
| **ネイティブ解像度** | 9504 x 6336 (61MP) |
| **ピクセルピッチ** | 3.76 μm |
| **drizzle後解像度** | 9728 x 6656 |
| **実効ピクセルピッチ** | ≈ 3.67 μm (9504/9728 × 3.76) |
| **中心ピクセルスケール** | ~54.4 arcsec/pixel |

### 3.2 各パラメータの影響度

| パラメータ | 影響度 | 用途 | 説明 |
|-----------|--------|------|------|
| **ピクセルピッチ** (μm) | **高** | スケール計算 | `arcsec/px = 206.265 * pitch / focal_length`。正確なスケールヒントに不可欠 |
| **焦点距離** (mm) | **高** | スケール計算、FOV計算 | ピクセルスケールとFOVの基本パラメータ |
| **センサーサイズ** (mm) | **中** | FOV計算、ピクセルピッチ推定 | `pixel_pitch = sensor_width / pixel_count` のフォールバック計算に使用 |
| **レンズ有効口径** (mm) | **低** | 直接影響なし | レンズ名の特定に間接的に役立つ |
| **撮影時の絞り** (f/) | **低〜中** | lensfun歪み補正 | lensfunでビネット/歪み補正を行う場合に必要。ソルブの幾何学には直接影響しない |

### 3.3 ピクセルピッチと焦点距離

**最重要パラメータ**。正確なピクセルスケール計算の根幹。

```python
# 現在の計算方法（main.py）
pixel_scale = 206.265 * (sensor_width / image_width) / focal_length * 1000

# ピクセルピッチ直接指定（より正確）
pixel_scale = 206.265 * pixel_pitch_um / focal_length
```

**注意**: drizzle後の画像は元の物理ピクセルピッチとは異なるため、`--pixel-scale` での直接指定が最も確実。

#### α7RV の場合
- ネイティブ解像度: 9504x6336 (61MP)
- ピクセルピッチ: 3.76 μm
- drizzle後 9728x6656: 実効ピクセルピッチ ≈ 3.67 μm（9504/9728 * 3.76）
- 14mm での理論スケール: `206.265 * 3.67 / 14 = 54.0 arcsec/pixel`（現在使用中の54.4と近い）

### 3.4 有効口径・絞り

プレートソルブの幾何学的精度には**直接影響しない**。ただし:

- **星像 SNR**: 開放ではより暗い星が写り、星の検出数が増える（ソルブ成功率に間接的影響）
- **ビネット（周辺減光）**: 開放（F2.0）では周辺減光が強くなり、エッジタイルの星検出が減少する可能性
- **lensfun歪み補正**: `mod.initialize(focal_length, aperture, ...)` のパラメータとして必要

---

## 4. lensfun レンズ歪みデータ（SEL14F18GM）

### 4.1 lensfun データベース登録情報

Sony FE 14mm f/1.8 GM (SEL14F18GM) は lensfun データベースに**登録済み**。

```
カメラ:   Sony ILCE-7RM5 (crop_factor=1.0, mount=Sony E)
レンズ:   Sony FE 14mm f/1.8 GM
タイプ:   RECTILINEAR（レクティリニア/正距投影）
焦点距離: 14.0mm（単焦点）
絞り範囲: f/1.8 - f/16.0
```

### 4.2 歪曲収差キャリブレーション

**モデル**: PT Lens (PTLENS)

```
r_corrected = a·r³ + b·r² + c·r + d·r
  ここで r は正規化半径（対角線半分で正規化）
  d = 1 - a - b - c

パラメータ (focal = 14.0mm):
  a =  0.01733   (正の3次項)
  b = -0.06263   (負の2次項)
  c =  0.05484   (正の1次項)
  d =  0.99046   (残余項)
```

歪みプロファイルは**弱い樽型**。中間半径（r_norm ≈ 0.5）付近で最大 ~1.8% のスケール増加があり、コーナー（r_norm = 1.0）では定義上 scale = 1.0 に戻る。

### 4.3 色収差 (TCA) キャリブレーション

**モデル**: POLY3 (3次多項式)

```
focal = 14.0mm
Red channel:   vr = 0.999973, cr = 0.000169
Blue channel:  vb = 1.000006, cb = 0.000089
```

TCA は非常に小さく（サブピクセルレベル）、プレートソルブには影響しない。

### 4.4 ビネッティングキャリブレーション

**モデル**: PA (Polynomial, 6th degree)

F2.0 撮影時のパラメータ:
```
V(r) = 1 + k1·r² + k2·r⁴ + k3·r⁶
  k1 = -1.3362
  k2 =  0.8269
  k3 = -0.2346
```

コーナーでの周辺光量低下は ~30-40%。エッジタイルでの暗い星の検出数に影響する可能性がある。

### 4.5 歪み量の定量分析（ネイティブ解像度 9504x6336）

lensfunpy `apply_geometry_distortion()` による補正マップから算出:

| 位置 | 中心からの距離 | 歪み量 (px) | 角度ずれ | 備考 |
|------|--------------|-------------|----------|------|
| 中心 | 0 px | 0.3 | 17" | 無視可能 |
| 上辺中央 | 3168 px | 6 | 338" (5.6') | 小 |
| 左辺中央 | 4752 px | 57 | 3093" (51.6') | 中 |
| 右上コーナー | 5710 px | 84 | 4578" (76.3') | 大 |

### 4.6 タイル内差分歪み分析（プレートソルブへの真の影響）

プレートソルブで重要なのは絶対歪み量ではなく、**タイル内での星位置の相対的なずれ**（差分歪み）。タイル中心を基準とした最大差分歪みを算出:

#### 8x8 グリッド（タイル 1188x792 px）— 主要タイルの差分歪み

| タイル | 位置 | 差分歪み (px) | 角度 (arcsec) | タイルFOV比 |
|--------|------|--------------|---------------|-------------|
| (3,3) | 中心 | **4.4** | 242 | **0.37%** |
| (3,4) | 中心 | **4.4** | 242 | **0.37%** |
| (2,2) | 内部 | 5.7 | 309 | 0.48% |
| (1,1) | 内部 | 22.8 | 1239 | 1.92% |
| (0,0) | コーナー | **25.2** | 1372 | **2.12%** |
| (7,7) | コーナー | **25.5** | 1387 | **2.15%** |
| (0,3) | エッジ | 23.2 | 1260 | 1.95% |

#### 5x5 グリッド（タイル 1900x1267 px）

| タイル | 位置 | 差分歪み (px) | 角度 (arcsec) | タイルFOV比 |
|--------|------|--------------|---------------|-------------|
| (2,2) | 中心 | **2.3** | 123 | **0.12%** |
| (1,1) | 内部 | 16.6 | 903 | 0.87% |
| (0,0) | コーナー | **35.1** | 1907 | **1.85%** |
| (4,4) | コーナー | **36.7** | 1997 | **1.93%** |

#### 3x3 グリッド（タイル 3168x2112 px）

| タイル | 位置 | 差分歪み (px) | 角度 (arcsec) | タイルFOV比 |
|--------|------|--------------|---------------|-------------|
| (1,1) | 中心 | **4.9** | 265 | **0.15%** |
| (0,0) | コーナー | **61.2** | 3330 | **1.93%** |

### 4.7 結論: SEL14F18GM の歪みはプレートソルブ失敗の主因ではない

**solve-field の quad パターンマッチングの歪み許容度は一般に ~5%**。

SEL14F18GM のタイル内差分歪みは:
- **中心タイル**: 0.1〜0.4% → **全く問題なし**
- **内部タイル**: 0.5〜2% → **許容範囲内**
- **コーナー/エッジタイル**: 2〜2.2% → **許容範囲内だが余裕は少ない**

つまり、このレンズの歪曲収差はプレートソルブ失敗の**主因ではない**。失敗の主因は:

1. **gnomonic投影の非線形性**（中心〜コーナーでスケール2倍以上の差 → ヒントのミスマッチ）
2. **インデックスファイルのクワッドスケール不足**（エッジタイルの大きなクワッドに非対応）
3. **地上景**（下端45%に星なし）

ただし、lensfunによる歪み補正は**精度向上に寄与**する。特に WCS 統合時の残差を減らし、最終的な座標精度を ~1' 以下に改善できる見込み。

---

## 5. レンズ歪み補正による改善提案

### 5.1 方法A: lensfun/lensfunpy による事前歪み補正

#### 概要

画像分割前にレンズの歪曲収差を除去し、正距投影（TAN）に近い画像を生成してからタイル分割・ソルブする。

#### 対応状況

Sony FE 14mm f/1.8 GM (SEL14F18GM) は **lensfun データベースに登録済み** — 歪み補正にすぐ利用可能。

#### 実装案

```python
# main.py の Step 1.5 として挿入
import lensfunpy
import cv2

def undistort_image(image_data, cam_maker, cam_model,
                    lens_maker, lens_model,
                    focal_length, aperture):
    db = lensfunpy.Database()
    cam = db.find_cameras(cam_maker, cam_model)[0]
    lens = db.find_lenses(cam, lens_maker, lens_model)[0]

    height, width = image_data.shape[:2]
    mod = lensfunpy.Modifier(lens, cam.crop_factor, width, height)
    mod.initialize(focal_length, aperture, float('inf'))

    undist_coords = mod.apply_geometry_distortion()

    # チャネルごとにリマップ
    result = np.empty_like(image_data)
    for c in range(image_data.shape[2]):
        result[:, :, c] = cv2.remap(
            image_data[:, :, c], undist_coords, None,
            cv2.INTER_LANCZOS4)
    return result
```

#### 期待効果
- **精度向上**: WCS残差を ~1' 以下に改善（現状 ~69' → 目標 <1'）
- **エッジタイルの成功率微増**: 差分歪み 2% → <0.5% に圧縮
- ただし歪みが主因ではないため、**ソルブ成功タイル数への劇的な改善は期待できない**（11 → 推定 12-14 タイル程度）

#### 制約・課題
- lensfunのレンズプロファイル精度はコミュニティ依存
- drizzle後の画像に適用する場合、ピクセルスケールの調整が必要
- 15mm魚眼レンズ（等距離射影）の場合、lensfunの歪みモデルが異なる可能性

### 5.2 方法B: solve-field の --predistort オプション

#### 概要

成功タイルの SIP 歪み係数を使い、失敗タイルのソルブ時に歪みを事前除去する。

```bash
solve-field --predistort center_tile.wcs \
  --crpix-center --tweak-order 4 \
  edge_tile.fits
```

#### 利点
- lensfun不要（外部依存なし）
- 画像自体の歪みデータ（SIP）を直接利用
- 実装が比較的簡単

#### 課題
- 1stパスで成功するタイルが必要（鶏と卵問題）
- タイル間で歪みの向きが異なるため、中心タイルのSIPがエッジタイルに適切とは限らない
- **タイル単位のSIPは画像全体の歪みモデルではない**

### 5.3 方法C: solve-field オプション最適化（即座に試行可能）

#### 現在のコードに追加すべきオプション

```python
# astrometry_local_solver.py の cmd 構築部分に追加
cmd.extend([
    '--crpix-center',       # 歪みの中心点を画像中心に固定
    '--tweak-order', '4',   # SIP次数を2→4に引き上げ
])
```

#### 期待効果
- `--crpix-center`: レンズの光学中心を基準にした歪みモデリング
- `--tweak-order 4`: より高次の歪みをフィッティング（精度 ~8" → ~2"）
- 既存のソルブ成功タイルでも精度が向上し、WCS統合の品質が改善

### 5.4 方法D: 大スケール用インデックスファイルの追加

#### 現状のインデックスファイル
- 4110-4119: 2000/Tycho2系、クワッドスケール 2'-35'
- 4214-4219: Tycho2系

#### 不足しているインデックス
エッジタイル（FOV 30-40°）のクワッドスケールをカバーするためのインデックス:
- **4100-4107**: プリセット0-7（大スケール、60'以上のクワッド）
- カスタムインデックス: `-d 3`（3星特徴量）で構築

```bash
# 大スケール用インデックスのダウンロード例
# http://data.astrometry.net/4100/ から
wget http://data.astrometry.net/4100/index-4107.fits  # ~30-60 arcmin quads
```

---

## 6. 15mm魚眼レンズ対応の考慮

### 6.1 魚眼レンズの投影モデル

魚眼レンズは正距投影（gnomonic/TAN）とは根本的に異なる投影を使用する:

| 投影タイプ | 数式 | 特徴 | 代表レンズ |
|-----------|------|------|-----------|
| **等距離射影** (equidistant) | r = f * θ | θに比例、全天可能 | 多くの魚眼 |
| **等立体角射影** (equisolid) | r = 2f * sin(θ/2) | 面積保存 | Sigma 8mm等 |
| **正射影** (orthographic) | r = f * sin(θ) | 周辺圧縮 | 稀 |
| **立体射影** (stereographic) | r = 2f * tan(θ/2) | 形状保存 | Samyang 12mm等 |
| **正距投影** (gnomonic/TAN) | r = f * tan(θ) | 直線保存、90°限界 | 通常のレンズ |

**15mm魚眼の場合**: FOV ~180° の場合は等距離射影が最も一般的。FOV ~110-120° の対角線魚眼なら等距離射影または等立体角射影。

### 6.2 魚眼レンズがプレートソルブに与える影響

1. **ピクセルスケールの非線形性がTANよりさらに極端**:
   - gnomonic: `scale ∝ 1/cos²(θ)` → θ→90°で無限大
   - equidistant: `scale ∝ θ/sin(θ)` → 中心と端でスケール差が小さい

2. **星のパターン変形**: 魚眼投影のクワッドパターンはTANカタログと大きく異なる

3. **solve-field のWCSモデルはTAN投影を前提**: SIP多項式で歪みを補正するが、魚眼の大きな歪みにはSIP次数5以上が必要で、不安定になりがち

### 6.3 魚眼レンズへの対応戦略

#### 戦略A: 歪み補正してTANに変換してからソルブ

```
魚眼画像 → lensfun/手動で等距投影に変換 → 正距投影画像 → タイル分割 → ソルブ
```

- **利点**: 既存のパイプラインがそのまま使える
- **課題**: FOV > 90° の場合、正距投影への変換で画像端が極端に引き伸ばされる

#### 戦略B: 立体射影（stereographic/STG）や正距方位図法（zenithal equidistant/ZEA）への変換

```
魚眼画像 → 立体射影に変換 → タイル分割 → ソルブ（CTYPE=RA---STG）
```

- **利点**: FOV > 90° でも安定した変換
- **課題**: solve-field は TAN+SIP を前提としているため、STG/ZEA での直接ソルブには追加対応が必要

#### 戦略C: 小タイル分割+高密度グリッド

```
魚眼画像 → 12x12 以上のグリッド分割 → 各タイルのFOVを <15° に抑制 → ソルブ
```

- **利点**: 各タイルが十分に小さければ、どの投影でもTAN近似が成り立つ
- **課題**: タイル数増加による処理時間増大、エッジタイルの星数減少

#### 推奨アプローチ

**lensfunでの事前歪み補正（戦略A）+ 適切なグリッドサイズ（戦略C）の組み合わせ** が最も実用的:

1. lensfun で魚眼歪みを正距投影に補正
2. 補正後の画像を適切なグリッド（10x10以上）で分割
3. 各タイルのFOVを ~15° 以下に抑制
4. 通常のTAN+SIPでソルブ

---

## 7. 改善ロードマップ（優先順位順）

### Phase 1: 即座に実行可能（コード変更最小）

1. **solve-field オプション最適化**
   - `--crpix-center` と `--tweak-order 4` を追加
   - 変更箇所: `python/solvers/astrometry_local_solver.py`
   - 期待効果: 既存成功タイルの精度向上、境界タイルの成功率向上

2. **2ndパスの改善**
   - タイムアウトを短縮（600s → 120s）
   - 成功タイルのスケールから周辺タイルのスケール範囲を推定してヒントに使用
   - 偽陽性フィルタ: ソルブ結果のスケールとWCSヒントのRADECの整合性チェック

3. **大スケール用インデックスファイルの追加**
   - 4100シリーズのプリセット4-7をダウンロード
   - エッジタイルの大きなクワッドスケールに対応

### Phase 2: 中期的改善（新機能追加）

4. **lensfunpy 統合**
   - `--undistort` CLIオプション追加
   - `--camera-maker`, `--camera-model`, `--lens-maker`, `--lens-model` オプション
   - Step 1.5 として歪み補正ステップを挿入
   - 変更箇所: `python/main.py`, 新規 `python/utils/lens_correction.py`

5. **魚眼レンズ対応**
   - 投影モデルの自動判定（レンズ情報から）
   - equidistant → gnomonic 変換の実装
   - 適応的グリッドサイズ（FOV/投影に基づく自動計算）

### Phase 3: 高度な改善

6. **自己キャリブレーション**
   - 1stパス成功タイルのWCSから画像全体の歪みモデルを推定
   - 推定歪みモデルを `--predistort` として2ndパスに適用
   - 反復的に精度を向上

7. **カスタムインデックスファイル生成**
   - 超広角用に `-d 3`（3星特徴量）でインデックスを構築
   - 大スケール用クワッドを含むインデックスの自動生成

---

## 8. 必要な追加情報

以下の情報があると、改善の方向性をさらに具体化できます:

1. ~~**レンズのモデル名**~~ → **済**: Sony FE 14mm f/1.8 GM (SEL14F18GM)
2. **友人の15mm魚眼レンズのモデル名**
   - 投影タイプの特定（等距離射影/等立体角射影等）
   - lensfun対応状況の確認
3. ~~**撮影時の絞り値**~~ → **済**: F2.0
4. **元画像のピクセル数（drizzle前）**
   - drizzle倍率の特定、正確なピクセルピッチ計算
5. **PixInsight での drizzle設定**
   - ドロップサイズ、出力ピクセルスケール

---

## 参考資料

- [Astrometry.net README](https://astrometry.net/doc/readme.html)
- [Building Index Files for Astrometry.net](https://astrometry.net/doc/build-index.html)
- [solve-field man page](https://manpages.debian.org/testing/astrometry.net/solve-field.1.en.html)
- [Solving precision - SIP coefficients (Google Groups)](https://groups.google.com/g/astrometry/c/IqoVZOpdVm0)
- [Astrometry.net Lens distortion discussion](https://groups.google.com/g/astrometry/c/h4icxKWIIoQ)
- [lensfunpy GitHub](https://github.com/letmaik/lensfunpy)
- [Lensfun corrections documentation](https://lensfun.github.io/manual/latest/corrections.html)
- [Lensfun supported lens list](https://lensfun.github.io/lenslist/)
- [PixInsight Solver Distortion Correction](https://pixinsight.com/tutorials/solver-distortion/)
